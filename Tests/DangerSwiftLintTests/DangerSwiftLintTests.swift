import XCTest
@testable import danger_swiftlint

class danger_swiftlintTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(danger_swiftlint().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
